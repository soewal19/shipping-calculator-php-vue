{
  "name": "shipping-calculator-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for Shipping Cost Calculator",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
